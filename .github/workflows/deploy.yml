name: Reusable Deploy

on:
  workflow_call:
    inputs:
      ref:
        description: Git ref or commit SHA to deploy.
        required: true
        type: string
      environment:
        description: GitHub deployment environment name.
        required: false
        default: development
        type: string
      deployment_description:
        description: GitHub deployment description.
        required: false
        default: Deploy to Kubernetes
        type: string
      kube_namespace:
        description: Kubernetes namespace to restart.
        required: false
        default: ''
        type: string
      kube_service_name:
        description: Kubernetes deployment and service name.
        required: false
        default: ''
        type: string
      kube_app_url:
        description: Public app URL to attach to the deployment status.
        required: false
        default: ''
        type: string
    secrets:
      KUBE_CONFIG:
        required: true
      KUBE_NAMESPACE:
        required: false
      KUBE_SERVICE_NAME:
        required: false
      KUBE_APP_URL:
        required: false

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      KUBE_NAMESPACE: ${{ inputs.kube_namespace || secrets.KUBE_NAMESPACE }}
      KUBE_SERVICE_NAME: ${{ inputs.kube_service_name || secrets.KUBE_SERVICE_NAME }}
      KUBE_APP_URL: ${{ inputs.kube_app_url || secrets.KUBE_APP_URL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Create GitHub deployment
        id: deployment
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          DEPLOY_REF: ${{ inputs.ref }}
          DEPLOY_ENVIRONMENT: ${{ inputs.environment }}
          DEPLOY_DESCRIPTION: ${{ inputs.deployment_description }}
        with:
          script: |
            const deployment = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: process.env.DEPLOY_REF,
              environment: process.env.DEPLOY_ENVIRONMENT,
              description: process.env.DEPLOY_DESCRIPTION,
              auto_merge: false,
              required_contexts: []
            });
            return deployment.data.id;
          result-encoding: string

      - name: Set deployment status to in_progress
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: Number('${{ steps.deployment.outputs.result }}'),
              state: 'in_progress',
              description: 'Deployment in progress'
            });

      - name: Install kubectl
        uses: azure/setup-kubectl@829323503d1be3d00ca8346e5391ca0b07a9ab0d # v5.1.0
        with:
          version: v1.29.6

      - name: Set up kubectl
        shell: bash
        run: |
          set -euo pipefail
          umask 177
          echo "$KUBE_CONFIG" | base64 -d > /tmp/kubeconfig
          chmod 600 /tmp/kubeconfig
          echo "KUBECONFIG=/tmp/kubeconfig" >> "$GITHUB_ENV"
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}

      - name: Validate Kubernetes identifiers
        shell: bash
        run: |
          set -euo pipefail

          validate_dns_label() {
            local name="$1"
            local value="$2"
            local pattern="$3"

            if [[ -z "$value" ]]; then
              echo "$name is required"
              exit 1
            fi

            if ((${#value} > 63)); then
              echo "$name must be 63 characters or fewer"
              exit 1
            fi

            if [[ ! "$value" =~ $pattern ]]; then
              echo "$name must be a valid Kubernetes DNS label"
              exit 1
            fi
          }

          validate_dns_label KUBE_NAMESPACE "$KUBE_NAMESPACE" '^[a-z0-9]([-a-z0-9]*[a-z0-9])?$'
          validate_dns_label KUBE_SERVICE_NAME "$KUBE_SERVICE_NAME" '^[a-z]([-a-z0-9]*[a-z0-9])?$'

      - name: Rollout restart deployment
        shell: bash
        run: |
          set -euo pipefail
          kubectl rollout restart deployment/"$KUBE_SERVICE_NAME" -n "$KUBE_NAMESPACE"

      - name: Wait for rollout to complete
        shell: bash
        run: |
          set -euo pipefail
          kubectl rollout status deployment/"$KUBE_SERVICE_NAME" -n "$KUBE_NAMESPACE" --timeout=300s

      - name: Verify deployment
        shell: bash
        run: |
          set -euo pipefail
          kubectl get pods -n "$KUBE_NAMESPACE" -l "app=$KUBE_SERVICE_NAME"
          kubectl get service "$KUBE_SERVICE_NAME" -n "$KUBE_NAMESPACE"

      - name: Set deployment status to success
        if: success()
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const deploymentId = Number('${{ steps.deployment.outputs.result }}');
            const status = {
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state: 'success',
              description: 'Deployment completed successfully',
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              auto_inactive: true
            };
            const envUrl = process.env.KUBE_APP_URL;
            if (envUrl) {
              status.environment_url = envUrl;
            }
            await github.rest.repos.createDeploymentStatus(status);

      - name: Set deployment status to failure
        if: ${{ (failure() || cancelled()) && steps.deployment.outputs.result }}
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const deploymentId = Number('${{ steps.deployment.outputs.result }}');
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state: 'failure',
              description: 'Deployment failed or cancelled',
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });
